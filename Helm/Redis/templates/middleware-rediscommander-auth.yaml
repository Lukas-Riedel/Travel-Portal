{{- if .Values.rediscommander.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rediscommander-auth-middleware
spec:
  basicAuth:
    secret: rediscommander-password
{{- end }}
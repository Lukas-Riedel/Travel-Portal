{{- if .Values.libretranslate.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: {{ .Values.libretranslate.service.type }}
  ports:
    - port: {{ .Values.libretranslate.service.port }}
      targetPort: {{ .Values.libretranslate.service.port }}
  selector:
    app: {{ .Release.Name }}
{{- end }}

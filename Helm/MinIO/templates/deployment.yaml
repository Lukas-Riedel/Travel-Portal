{{- if .Values.minio.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
spec:
  replicas: {{ .Values.minio.replicas }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      nodeSelector:
        type: light
      containers:
        - name: {{ .Release.Name }}
          resources:
            requests:
              cpu: 50m
              memory: 400Mi
          image: "{{ .Values.minio.image }}:{{ .Values.minio.tag }}"
          args:
            - server
            - /data
            - --console-address
            - ":{{ .Values.minio.service.console.port }}"
          ports:
            - name: s3
              containerPort: {{ .Values.minio.service.s3.port }}
            - name: http-console
              containerPort: {{ .Values.minio.service.console.port }}
          readinessProbe:
            httpGet:
              path: /minio/health/ready
              port: {{ .Values.minio.service.s3.port }}
            initialDelaySeconds: 15
            periodSeconds: 15
            failureThreshold: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /minio/health/live
              port: {{ .Values.minio.service.s3.port }}
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 10
            timeoutSeconds: 5
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secret
                  key: minio-access-key
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secret
                  key: minio-password
            - name: MINIO_BROWSER_REDIRECT_URL
              value: "https://{{ .Values.minio.service.console.host }}"
          volumeMounts:
            - name: {{ .Release.Name }}-data
              mountPath: /data
      volumes:
        - name: {{ .Release.Name }}-data
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-pvc
{{- end }}
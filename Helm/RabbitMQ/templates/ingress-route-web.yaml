{{- if .Values.rabbitmq.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}-web-router
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ .Values.rabbitmq.service.management.host }}`)
      kind: Rule
      services:
        - name: {{ .Release.Name }}
          port: {{ .Values.rabbitmq.service.management.port }}
  tls:
    certResolver: letsencrypt
{{- end }}
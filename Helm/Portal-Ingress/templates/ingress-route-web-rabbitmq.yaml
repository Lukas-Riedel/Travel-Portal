apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rabbitmq-web-router
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`rmq.lriedel.fun`)
      kind: Rule
      services:
        - name: rabbitmq
          port: 15672
  tls:
    certResolver: letsencrypt
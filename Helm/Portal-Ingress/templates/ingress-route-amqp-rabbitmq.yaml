apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: rabbitmq-amqp-router
spec:
  entryPoints:
    - amqp
  routes:
    - match: HostSNI(`lriedel.fun`)
      services:
        - name: rabbitmq
          port: 5672
  tls:
    certResolver: letsencrypt
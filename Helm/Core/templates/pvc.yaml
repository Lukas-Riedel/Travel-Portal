{{- if .Values.core.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: core-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
{{- end }}
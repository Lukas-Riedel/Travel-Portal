{{- if .Values.core.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}-web-router
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ .Values.core.service.host }}`)
      kind: Rule
      services:
        - name: {{ .Release.Name }}
          port: {{ .Values.core.service.port }}
  tls:
    certResolver: letsencrypt
{{- end }}